using UnityEngine;

namespace Util
{
    public static class GeometryConsts
    {
        public static readonly float Sqrt2 = Mathf.Sqrt(2);
        public static readonly float Sqrt2Inverted = 1f/Mathf.Sqrt(2);
        
        public static readonly float Sqrt3 = Mathf.Sqrt(3);
        public static readonly float Sqrt3Inverted = 1f/Mathf.Sqrt(3);
    }
}